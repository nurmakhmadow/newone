let arr = [1, 2, false, 'hello', 24, 'world', undefined, null, 'error', 22]

let studio = arr.filter(item => {
    if (Number.length >= 5){
 console.log(
    'good'
 );
    
    } else {
        console.log(
            'bad'
        );
    }

})